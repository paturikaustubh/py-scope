/** The VS Code configuration section key for all PyScope settings. */
export const CONFIG_SECTION = "pyScope";

/** Fallback defaults used when a setting is missing or invalid. */
export const DEFAULTS = {
  color: "27, 153, 5",
  blockOpacity: 0.08,
  firstLastOpacity: 0.2,
} as const;
